{% spaceless %}
    <div class="modal fade in" id="{{ modal.payment_history_id }}">
        <div class="modal-dialog {{ modal.payment_history.size|default() }}">

            <div class="modal-content">

                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                    <h4 class="modal-title">
                        <i class="fa fa-fw fa-file-text-o"></i> {{ modal.payment_history.title|default("Datos") }}
                        <small class="label bg-light-blue-active" style="font-size: 65%;"></small>
                        <i class="fa fa-refresh fa-spin" style="color: white; margin-left: 10px; display:none"></i>
                    </h4>
                </div>

                <form name="{{ form_mapper.payment_history }}" method="post">
                    <div class="crud-modal-content"></div>
                </form>
            </div>

        </div>
    </div>
{% endspaceless %}